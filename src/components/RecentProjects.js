import React from "react";

const RecentProjects = () => (
  <section className="text-center text-white p-10">
    <h2 className="text-4xl font-bold text-purple-400">Recent Projects</h2>
  </section>
);

export default RecentProjects;